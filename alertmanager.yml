global:
  resolve_timeout: 5m

route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 2m
  receiver: jenkins-rollback-webhook

receivers:
  - name: jenkins-rollback-webhook
    webhook_configs:
      - url: 'http://32.198.95.64:8080/generic-webhook-trigger/invoke?token=ROLLBACK_01189D_TOKEN'
        http_config:
          basic_auth:
            username: 'admin'
            password: '11221b8ab9ad6af8f2bc821ac0d439851b'
        send_resolved: false
